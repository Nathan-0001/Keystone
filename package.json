{
  "name": "keystone-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx live-server --port=5500 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
